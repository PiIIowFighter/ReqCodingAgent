# 最终实验结果

本目录只保留三次迭代的最终摘要、必要的环境/隔离凭据与最终对比报告。逐次运行、开发记录和失败诊断属于过程产物，可在清理前提交 `a4eac341ae1f59d04b24d992d3df50fc7a7ddb00` 的 Git 历史中核验。

- `iteration1/`：可复现评测环境的 smoke 与隔离证明。
- `iteration2/`：基础 Coding Agent 的 baseline-v1 正式结果。
- `iteration3/`：需求增强后的 baseline-v3 最终结果及 v1→v3 对比。

本次整理只改变仓库呈现结构，不改写冻结配置、Ontology、报告中的实验数值或既有 Git 历史。
