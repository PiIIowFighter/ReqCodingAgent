# Stock search fixture

This is a small static page backed by local simulated stock data. It intentionally starts with browsing only; the demo task adds search without introducing a build step or network dependency.
